import {component$} from '@builder.io/qwik';

type IndexProps = {

}

export default component$((props:IndexProps) => {

    const {

    } = props


    return (
        <div>
            editor
        </div>
    );
});
